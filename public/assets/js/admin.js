// Admin JS — stubs for Chart.js, Leaflet
// Charts will be initialized when stats page loads
document.addEventListener('htmx:afterSwap', function () {
    if (document.getElementById('viewsChart')) {
        // init Chart.js
    }
    if (document.getElementById('geoMap')) {
        // init Leaflet map
    }
});
